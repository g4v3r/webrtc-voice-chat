# 6272 — голосовые комнаты

Современное веб‑приложение для голосового общения в комнатах.  
Через браузер, без установки клиента: WebRTC‑аудио, WebSocket‑сигналинг, отдельный TURN/STUN‑сервер на VPS.

---

## Что умеет 6272

- Голосовой чат в комнатах (несколько участников, mesh WebRTC).
- Список комнат слева с количеством людей и значком 🔒 для запароленных.
- Пароль на комнату, никнеймы, статус «говорит / мьют» у каждого.
- Настройки микрофона (эхоподавление, шумоподавление, авто‑гейн) с автосохранением.
- Авто‑reconnect при обрыве WebSocket и подсказки по состоянию соединения.
- TURN‑сервер на VPS для работы через интернет, а не только в локалке.
- Постоянный `userId` и роль «создателя комнаты» с возможностью кикать участников.
- Пинг до сервера и статус связи под ником каждого участника.

---

## Структура проекта

- `server.js`
  - Node.js + Express + `ws`.
  - Раздаёт статические файлы из `public/`.
  - Держит в памяти комнаты и участников: `rooms` и `clients`.
  - Обрабатывает сообщения типов: `join`, `signal`, `state`, `ping`, `kick`.
  - Шлёт `room-state` (состав комнаты, ники, мьют, пинг, статус соединения, флаг владельца).
  - Шлёт `rooms` — список всех комнат (для левой панели).
  - Валидирует входные данные и ограничивает частоту сообщений (простое rate limiting).

- `public/index.html`
  - Чистый HTML + CSS в одном файле.
  - Двухколоночный интерфейс в духе голосовых зон:
    - слева — список комнат и кнопка «Новая»;
    - справа — текущая зона: заголовок 6272, поля ID/пароль, кнопки подключиться/отключиться/микрофон;
    - ниже — карточки участников с крупными аватарами‑кружками, ником, пингом и кнопкой «Кик» (у создателя).
  - Адаптивная вёрстка: на телефоне панели идут друг за другом, блоки складываются в столбики.

- `public/client.js`
  - Вся логика клиента: WebSocket‑сигналинг, WebRTC, UI‑состояние.
  - Подключение к серверу: `ws://<host>` или `wss://<host>` в зависимости от протокола.
  - HTTP‑запрос `/api/rooms` для первичного получения списка комнат + периодический refresh.
  - WebRTC:
    - `RTCPeerConnection` с ICE‑серверами (TURN + STUN);
    - mesh‑топология (каждый с каждым), оффер инициирует участник с меньшим `clientId`;
    - динамическая замена трека через `replaceTrack` при изменении настроек микрофона.
  - Настройки пользователя:
    - `userId` генерируется один раз и хранится в `localStorage`;
    - ник и флаги аудио‑настроек тоже лежат в `localStorage` и подхватываются при старте;
    - модальное окно «Настройки» позволяет менять ник и аудио‑опции.
  - Ping:
    - раз в 5 секунд отправляется `{ type: "ping", ts: Date.now() }`;
    - сервер считает RTT, сохраняет `pingMs` и шлёт обновлённый `room-state`;
    - клиент показывает пинг под ником (`NN ms` либо `нет связи`).

---

## Локальный запуск для разработки

Требуется Node.js (желательно LTS, например 20.x).

```bash
cd webrtc-voice-chat
npm install
npm start
```

После этого приложение будет доступно на `http://localhost:3000`.
Открой страницу в двух браузерах / на двух устройствах и зайдите в одну комнату, чтобы проверить голос.

---

## TURN/STUN‑сервер на VPS (coturn)

Чтобы голос стабильно работал между разными сетями и за NAT, нужен TURN‑сервер.  
Мы используем отдельный VPS с Ubuntu и `coturn`.

### Минимальные требования к VPS

Для аудио‑чата до ~1000 говорящих (в разных комнатах) разумный старт:

- 4–8 vCPU;
- 8–16 ГБ RAM;
- 1 Gbps порт, от 5–10 ТБ трафика/мес;
- любой современный SSD.

### Установка coturn (кратко)

```bash
apt update && apt upgrade -y
apt install -y coturn
```

Пример `/etc/turnserver.conf`:

```text
listening-port=3478
fingerprint
lt-cred-mech
realm=example.com

user=voiceuser:veryStrongPassword123

total-quota=1000
bps-capacity=100000000

no-loopback-peers
no-multicast-peers
```

`/etc/default/coturn`:

```text
TURNSERVER_ENABLED=1
```

Запуск и автозапуск:

```bash
systemctl enable --now coturn
systemctl status coturn
```

Не забудь открыть порт `3478` (UDP/TCP) в firewall и панели провайдера.

### ICE‑настройки на клиенте

В `public/client.js` при создании `RTCPeerConnection` используется конфигурация ICE.  
Замените значения на свои:

```js
const pc = new RTCPeerConnection({
  iceServers: [
    {
      urls: [
        'turn:YOUR_VPS_IP:3478?transport=udp',
        'turn:YOUR_VPS_IP:3478?transport=tcp',
        'stun:YOUR_VPS_IP:3478',
      ],
      username: 'voiceuser',
      credential: 'veryStrongPassword123',
    },
    { urls: 'stun:stun.l.google.com:19302' },
  ],
});
```

---

## Как продолжать работать с проектом

1. **Разработка**
   - `npm start` — локальный сервер на `http://localhost:3000`.
   - Меняешь `server.js`, `public/index.html`, `public/client.js`.
   - `npm run lint` / `npm run format` — проверка/форматирование кода.

2. **Деплой фронтенда и сигналинга**
   - Код лежит в GitHub‑репозитории.
   - Render (или другой PaaS) тянет репозиторий, запускает `node server.js`.
   - Адрес вида `https://твой‑сервис.onrender.com` используют все клиенты.

3. **Обновление TURN**
   - TURN на VPS настраивается один раз.
   - При смене IP / логина / пароля — обнови блок `iceServers` в `client.js` и задеплой фронтенд.

---

## Краткая история развития 6272

- От простого P2P‑прототипа между двумя участниками — до комнат с несколькими людьми.
- Добавлены пароль на комнату, роли (создатель), кик участников.
- Настройки звука и никнейма вынесены в модальное окно с автосохранением.
- Подключён TURN‑сервер на VPS для работы через интернет.
- Реализован список комнат слева и интерфейс в стиле голосовых зон, адаптивный под ПК и телефон.
- Добавлены постоянный `userId`, ping/pong и отображение пинга/статуса связи под каждым ником.
